---
- hosts: all
  become: all
  tasks:
  - name: i want to install apache2
    apt:
    - name: apache2
      state: present

  - name: i want to run
    service:
    - name: apache2
      state: started
