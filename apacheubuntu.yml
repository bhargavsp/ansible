---
- hosts: linux
  become: yes
  tasks:
 
  - name: install webserver
    yum:
      name: httpd
      state: present
  - name: start webserver
    service:
      name: httpd
      state: started

  - name: install tree
    apt:
      name: tree
      state: present
    ignore_errors: yes

    

